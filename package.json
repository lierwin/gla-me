{
  "name": "galaxycloud-meteor",
  "version": "1.0.0",
  "description": "GalaxyCloud Meteor subscription generator",
  "private": true,
  "scripts": {
    "start": "meteor run --port $PORT"
  },
  "dependencies": {
    "meteor-node-stubs": "^2.1.0",
    "axios": "latest"
  },
  "engines": {
    "node": ">=14"
  }
}
